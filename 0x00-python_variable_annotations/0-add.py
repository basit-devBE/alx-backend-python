#!/usr/bin/env python3

# Define a function called `add` that takes two arguments, `a` and `b`, both of type `float`.
# The function returns the sum of `a` and `b` as a `float`.
def add(a: float, b: float) -> float:
    # Simply return the result of adding `a` and `b`.
    return a + b
